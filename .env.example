# Docent API key for trajectory ingestion
# Get your API key from https://docent.dev
DOCENT_API_KEY=your_api_key_here

# Optional: Custom GCS bucket for trajectories
# GSO_GCS_BUCKET=gs://your-bucket-name
